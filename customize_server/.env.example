# Server
PORT=3001
API_BASE_URL=/api/v1

# MongoDB
MONGODB_URI=mongodb://localhost:27017/xrt

# JWT
JWT_SECRET=your_jwt_secret_min_32_chars

# Cloudinary – used for all image uploads (items, categories, hero slides, logo)
# Get these from https://cloudinary.com/console
CLOUDINARY_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Optional: set to disk to use local uploads/ instead of Cloudinary (not recommended on Vercel)
# ATTACHMENT_STORAGE=cloudinary

# CORS (comma-separated)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173
