/** Shared layout: cards side-by-side (wraps), max 4 — 2 columns on small screens, up to 4 on large. */
export const PROMOTIONS_ROW_CLASSES =
  'mx-auto grid w-full max-w-[1260px] grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6';

export const PROMOTIONS_CELL_CLASSES = 'min-w-0';
